# Run OpenVPN in client mode
client

# Use a TUN virtual interface (layer 3 IP tunnel)
dev tun

# Use TCP as transport protocol
proto tcp

# =====================================================
# Remote VPN endpoint
# pc2 connects directly to the public address of r10
# on TCP port 7070. This is the standard OpenVPN setup
# without SSH port forwarding.
# =====================================================

remote 151.0.45.1 7070

# =====================================================
# Public Key Infrastructure (PKI)
# =====================================================

# Certificate Authority certificate
ca /root/ca.crt

# Client certificate (unique for pc2)
cert /root/vpn_client_2.crt

# Client private key
key /root/vpn_client_2.key

# =====================================================
# Keep VPN interface and keys active across restarts
# =====================================================

# Do not re-read key across restarts
persist-key

# Do not close and reopen the TUN interface on restart
persist-tun

# Set verbosity level for logging
verb 3